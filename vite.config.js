// Vite settings. Vite is the tool that runs the local dev server and bundles the game.
// We keep this minimal; the default settings are fine for a Phaser project.
import { defineConfig } from 'vite';

export default defineConfig({
  // Serve the project at the root path.
  base: './',
});
